name: VxLAN-Overlay-Network-in-AWS
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A minimal AWS Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
